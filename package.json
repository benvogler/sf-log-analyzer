{
  "name": "sf-log-analyzer",
  "version": "0.0.1",
  "description": "",
  "main": "server.ts",
  "scripts": {
    "start": "cd server && npm start",
    "serve": "concurrently -n \"client,server\" \"cd client && npm serve\" \"cd server && npm serve\"",
    "build": "cd client && npm run build && cd ../server && npm run build"
  },
  "author": "Ben Vogler",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^4.0.1"
  }
}
